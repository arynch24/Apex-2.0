/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: [
        "images.unsplash.com",
        "assets.aceternity.com", // Add multiple domains in a single array
      ],
    },
  };
  
  export default nextConfig;
  